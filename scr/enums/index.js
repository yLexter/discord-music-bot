const queueComponents = require("./queueComponents")
const songType = require("./songType")
const pagination = require("./pagination")
const geral = require("./geral")

module.exports = {
    queueComponents,
    songType,
    pagination,
    geral
}